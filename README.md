# SENTINAL
## Explaination:
### This is a malicious file that contains a few aspects of harm.IT will first work as a worm and replicate itself about a 100 times into the same directory and create a copy of the file into each of the directory.THen it will also allow for a reverseshell connetcion and also when you send it to the victim have the sever file running before hand as that will connect to the reverse shell looking for a connection and it will only work if you have the server file running first.
