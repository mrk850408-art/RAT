# SENTINAL
## Explaination:
### This is a malicious file that contains a few aspects of harm.IT will first work as a worm and replicate itself about a 100 times into the same directory and create a copy of the file into each of the directory.THen it will also allow for a reverseshell connetcion and also when you send it to the victim have the sever file running before hand as that will connect to the reverse shell looking for a connection and it will only work if you have the server file running first.Also make sure to enter the IP in the host section in  both files as that is key for this to work and it will also then create a file in the directory and since you hgave a connection you can access all their files etc and the file will be called Log_file and you can cat it to see information about the user such as network info and tehir ip etc.It also includes a keylogger and all the keys tehy are pressing will be saved in a file called keyfile.txt and you can also view it similar to the file earlier.
